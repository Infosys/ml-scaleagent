# FastAPI + SQLAlchemy + Postgres + Typer CLI

Scaffold with FastAPI app, SQLAlchemy ORM, and Typer CLI.

Usage: set DATABASE_URL in .env, install requirements, run uvicorn app.main:app, or CLI via python -m app.cli.app get.
